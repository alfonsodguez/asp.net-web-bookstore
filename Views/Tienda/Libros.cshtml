@model List<Libro>
<div class="container">
    @for (int i = 0; i < Model.Count; i = i + 3) {
        <partial name="_VistaParcialTupla3Libros" model="Model.Skip<Libro>(i).Take<Libro>(3).ToList<Libro>()" />
    }
</div>
